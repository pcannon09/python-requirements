[ install_dir = "./tests/pip_modules/" ]

remove.install_dir

[ conf_dir = "./tests/conf.py" ]

[ required_os = "darwin" ]
install "pyobjc"

[ required_os = "windows" ]
install "pywin32"

[ required_os = "linux" ]
install "pyxhook"

[ required_os = "any" ]
install "pygame"

